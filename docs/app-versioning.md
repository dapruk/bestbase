# App Versioning

Mode:

- `low`: deteksi mismatch dan update versi tersimpan.
- `mild`: clear sessionStorage dan app/query cache yang disiapkan.
- `aggressive`: clear localStorage, sessionStorage, CacheStorage, dan persisted
  state scoped aplikasi.
