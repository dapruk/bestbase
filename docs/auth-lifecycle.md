# Auth Lifecycle

State auth: `idle`, `checking`, `authenticated`, `guest`, `refreshing`,
`expired`, dan `error`. Default mode adalah cookie session. Token sensitif tidak
disimpan di storage browser.
