# Testing Strategy

Prioritas test adalah headless `.test.ts` untuk store, service, schema, guard,
fetcher helper, persistence, dan pure utils. Gunakan `.test.tsx` hanya saat perlu
memvalidasi rendering atau interaksi DOM.
